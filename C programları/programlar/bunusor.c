#include <stdio.h>
#include <ctype.h>
#include <string.h>
void bosluk_atla(FILE *stream)
{
     	char c;//
     	while(isspace(c)!=0)
     	c = getc(stream);
     	ungetc(c,stream);
}
int main()
{

  

















return 0;
}
